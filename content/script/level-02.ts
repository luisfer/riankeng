import { scriptWords } from '../build'

export default scriptWords(2, [
  ['máa', 'ม้า', ['horse'], 'n', ['bridge', 'voice:w:máa', 'parts:ม+้+า'], 'The mark sits on ม้า, the horse you already know. No mark on มา is the mid tone.'],
])
