# Server-only. Empty locally so npm run dev stays open.
# On Vercel, set this on Production and Preview.
SITE_PASSWORD=

# Server-only. The waitlist insert. Never put the service role in the browser.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Public. The course uses these to sign in and write that learner's progress row.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
